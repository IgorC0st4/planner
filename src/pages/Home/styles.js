import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    fab: {
        position: 'absolute',
        margin: 10,
        right: 0,
        bottom: 0,
    },
    loadingPage: {
        flex: 1,
        alignContent: 'center',
        alignItems: 'center',
        top: '50%',
    },
    page: {
        padding: 10,
        flex: 1,
    },
});

export default styles;
